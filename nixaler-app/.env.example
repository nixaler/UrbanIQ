# niXaler's own Supabase project — NOT the trivia game's or Intellectus's.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Direct Postgres connection string (Supabase: Project Settings > Database).
# Used by Drizzle; must be a plain TCP connection, not pooled/HTTP, since the
# app runs entirely on the Node.js runtime (see src/lib/db/client.ts).
DATABASE_URL=

# Stripe (fixed monthly plan subscription checkout + webhooks).
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PRICE_ID=
